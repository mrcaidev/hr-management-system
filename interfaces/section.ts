export interface Section {
  sectionId: string;
  sectionName: string;
  managerId: string;
  managerName: string;
  placeId: string;
  maxSalary: string;
  minSalary: string;
  averageSalary: string;
}
